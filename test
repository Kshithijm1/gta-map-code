    useEffect(() => {
        if (isOpen && gcsSelectionModel) {
            setPersistedSelectedIds(Array.isArray(gcsSelectionModel) ? gcsSelectionModel.map(id => String(id)) : []);
        } else if (!isOpen) {
            setPersistedSelectedIds([]);
        }
    }, [isOpen, gcsSelectionModel]);
