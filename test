/* ============================================
   Row-Based Folder Structure Table Styles
   Using display:table for guaranteed alignment
   ============================================ */

/* Container */
.tableContainer {
  display: flex;
  flex-direction: column;
  height: 100%;
  overflow-x: auto;
  overflow-y: auto;
}

/* ---- Table wrapper ---- */
.headerRow {
  display: table;
  table-layout: fixed;
  width: 100%;
  border-bottom: 2px solid #e0e0e0;
  background-color: #fafafa;
  font-weight: 600;
  font-size: 13px;
  color: #616161;
  user-select: none;
  position: sticky;
  top: 0;
  z-index: 2;
}

/* ---- Table Body ---- */
.tableBody {
  flex: 1;
}

/* ---- Table Row ---- */
.tableRow {
  display: table;
  table-layout: fixed;
  width: 100%;
  border-bottom: 1px solid #f0f0f0;
  cursor: pointer;
  transition: background-color 0.15s ease;
  user-select: none;
}

.tableRow:hover {
  background-color: rgba(25, 118, 210, 0.04);
}

.tableRow.selected {
  background-color: rgba(25, 118, 210, 0.10);
}

.tableRow.selected:hover {
  background-color: rgba(25, 118, 210, 0.15);
}

/* ============================================
   Column Cells — all table-cell with fixed widths
   ============================================ */

.cellCheckbox {
  display: table-cell;
  width: 36px;
  vertical-align: middle;
  text-align: center;
  padding: 6px 0;
}

.cellIcon {
  display: table-cell;
  width: 32px;
  vertical-align: middle;
  text-align: center;
  padding: 6px 0;
}

.cellName {
  display: table-cell;
  width: 220px;
  vertical-align: middle;
  padding: 6px 8px 6px 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.nameText {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 14px;
  color: #212121;
}

.cellSharing {
  display: table-cell;
  width: 50px;
  vertical-align: middle;
  text-align: center;
  padding: 6px 0;
}

.cellFavorite {
  display: table-cell;
  width: 36px;
  vertical-align: middle;
  text-align: center;
  padding: 6px 0;
}

.cellSize {
  display: table-cell;
  width: 70px;
  vertical-align: middle;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding: 6px 4px;
}

.cellType {
  display: table-cell;
  width: 50px;
  vertical-align: middle;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding: 6px 4px;
}

/* Tags — scrollable horizontally */
.cellTags {
  display: table-cell;
  width: 150px;
  vertical-align: middle;
  padding: 6px 4px;
  overflow: hidden;
}

.cellTags>div,
.cellTags>span {
  display: inline-block;
  white-space: nowrap;
}

.tag {
  display: inline-flex;
  align-items: center;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 500;
  background-color: #f0f4f8;
  color: #334155;
  border: 1px solid #e2e8f0;
  white-space: nowrap;
  transition: all 0.2s ease;
  margin-right: 4px;
}

/* Date Created — scrollable */
.cellDate {
  display: table-cell;
  width: 160px;
  vertical-align: middle;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding: 6px 4px;
}

.cellOwner {
  display: table-cell;
  width: 100px;
  vertical-align: middle;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding: 6px 4px;
}

.cellDelete {
  display: table-cell;
  width: 36px;
  vertical-align: middle;
  text-align: center;
  color: #757575;
  cursor: pointer;
  padding: 6px 0;
}

.cellDelete:hover {
  color: #d32f2f;
}

.cellSpacer {
  display: none;
}

/* ---- Icons ---- */
.folderIcon {
  color: #ffc107;
  font-size: 24px !important;
}

.fileIcon {
  font-size: 22px !important;
}

.pdfIcon {
  color: #f44336;
}

.csvIcon {
  color: #4caf50;
}

.imgIcon {
  color: #9c27b0;
}

.txtIcon {
  color: #607d8b;
}

.docIcon {
  color: #2196f3;
}

/* Favorite Star */
.favoriteIcon {
  cursor: pointer;
  font-size: 20px !important;
  transition: color 0.15s ease, transform 0.15s ease;
}

.favoriteIcon:hover {
  transform: scale(1.15);
}

.favoriteActive {
  color: #ffc107 !important;
}

.favoriteInactive {
  color: #bdbdbd !important;
}

.favoriteInactive:hover {
  color: #ffc107 !important;
}

/* Sharing icons */
.sharingIcon {
  font-size: 20px !important;
  color: #9e9e9e;
}

.personalIcon {
  color: #1976d2;
}

.teamIcon {
  color: #2e7d32;
}

/* Header column text */
.headerCell {
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: #9e9e9e;
  padding-top: 8px;
  padding-bottom: 8px;
}

/* Empty state */
.emptyState {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 200px;
  color: #9e9e9e;
}
