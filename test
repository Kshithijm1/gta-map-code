/* ============================================
   Row-Based Folder Structure Table Styles
   ============================================ */

/* Container */
.tableContainer {
  display: flex;
  flex-direction: column;
  height: 100%;
  overflow: hidden;
}

/* ---- Header Row ---- */
.headerRow {
  display: flex;
  align-items: center;
  padding: 8px 12px;
  border-bottom: 2px solid #e0e0e0;
  background-color: #fafafa;
  font-weight: 600;
  font-size: 13px;
  color: #616161;
  user-select: none;
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 2;
  /* Reserve space for body scrollbar so columns align */
  padding-right: calc(12px + 17px);
}

/* ---- Table Body ---- */
.tableBody {
  flex: 1;
  overflow-y: auto;
  overflow-x: hidden;
}

/* ---- Table Row ---- */
.tableRow {
  display: flex;
  align-items: center;
  padding: 6px 12px;
  border-bottom: 1px solid #f0f0f0;
  cursor: pointer;
  transition: background-color 0.15s ease;
  min-height: 44px;
  user-select: none;
}

.tableRow:hover {
  background-color: rgba(25, 118, 210, 0.04);
}

.tableRow.selected {
  background-color: rgba(25, 118, 210, 0.10);
}

.tableRow.selected:hover {
  background-color: rgba(25, 118, 210, 0.15);
}

/* ---- Column Cells ---- */
/* Fixed-width utility columns */
.cellCheckbox {
  flex: 0 0 36px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.cellIcon {
  flex: 0 0 32px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Flexible data columns — proportional sizing */
.cellName {
  flex: 1.5 1 0%;
  min-width: 120px;
  display: flex;
  align-items: center;
  padding-right: 12px;
  overflow: hidden;
}

.nameText {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 14px;
  color: #212121;
}

.cellSharing {
  flex: 0 0 44px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.cellFavorite {
  flex: 0 0 32px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Fixed-width data columns to keep them tight on the right */
.cellSize {
  flex: 0 0 60px;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding-right: 12px;
}

.cellType {
  flex: 0 0 60px;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding-right: 12px;
}

.cellTags {
  flex: 0 0 140px;
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: nowrap;
  overflow-x: auto;
  overflow-y: hidden;
  padding-right: 12px;
  -ms-overflow-style: none;
  scrollbar-width: none;
}

.cellTags::-webkit-scrollbar {
  display: none;
}

.tag {
  display: inline-flex;
  align-items: center;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 500;
  background-color: #f0f4f8;
  color: #334155;
  border: 1px solid #e2e8f0;
  white-space: nowrap;
  transition: all 0.2s ease;
}

.cellDate {
  flex: 0 0 120px;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding-right: 12px;
}

.cellOwner {
  flex: 0 0 100px;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding-right: 12px;
}

.cellDelete {
  flex: 0 0 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #757575;
  cursor: pointer;
}

.cellDelete:hover {
  color: #d32f2f;
}

.cellSpacer {
  flex: 1;
  min-width: 0;
}

/* ---- Icons ---- */
.folderIcon {
  color: #ffc107;
  font-size: 24px !important;
}

.fileIcon {
  font-size: 22px !important;
}

/* Specific File Type Colors */
.pdfIcon {
  color: #f44336;
}

.csvIcon {
  color: #4caf50;
}

.imgIcon {
  color: #9c27b0;
}

.txtIcon {
  color: #607d8b;
}

.docIcon {
  color: #2196f3;
}

/* Favorite Star */
.favoriteIcon {
  cursor: pointer;
  font-size: 20px !important;
  transition: color 0.15s ease, transform 0.15s ease;
}

.favoriteIcon:hover {
  transform: scale(1.15);
}

.favoriteActive {
  color: #ffc107 !important;
}

.favoriteInactive {
  color: #bdbdbd !important;
}

.favoriteInactive:hover {
  color: #ffc107 !important;
}

/* Sharing icons */
.sharingIcon {
  font-size: 20px !important;
  color: #9e9e9e;
}

.personalIcon {
  color: #1976d2;
}

.teamIcon {
  color: #2e7d32;
}

/* Header column text */
.headerCell {
  display: flex;
  align-items: center;
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: #9e9e9e;
}

/* Empty state */
.emptyState {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 200px;
  color: #9e9e9e;
}
