/* ============================================
   Row-Based Folder Structure Table Styles
   ============================================ */

/* Container */
.tableContainer {
  display: flex;
  flex-direction: column;
  height: 100%;
  overflow-x: auto;
  overflow-y: hidden;
}

/* ---- Shared Row Layout (used by BOTH header and data rows) ---- */
.headerRow,
.tableRow {
  display: flex;
  align-items: center;
  padding: 6px 12px;
  min-height: 44px;
}

/* ---- Header Row specifics ---- */
.headerRow {
  border-bottom: 2px solid #e0e0e0;
  background-color: #fafafa;
  font-weight: 600;
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: #9e9e9e;
  user-select: none;
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 2;
}

/* ---- Table Body ---- */
.tableBody {
  flex: 1;
  overflow-y: auto;
}

/* ---- Table Row specifics ---- */
.tableRow {
  border-bottom: 1px solid #f0f0f0;
  cursor: pointer;
  transition: background-color 0.15s ease;
  user-select: none;
}

.tableRow:hover {
  background-color: rgba(25, 118, 210, 0.04);
}

.tableRow.selected {
  background-color: rgba(25, 118, 210, 0.10);
}

.tableRow.selected:hover {
  background-color: rgba(25, 118, 210, 0.15);
}

/* ---- Column Cells ---- */
.cellCheckbox {
  width: 40px;
  min-width: 40px;
  max-width: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.cellIcon {
  width: 36px;
  min-width: 36px;
  max-width: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.cellName {
  flex: 1 1 0%;
  min-width: 120px;
  display: flex;
  align-items: center;
  gap: 8px;
  padding-right: 8px;
  overflow: hidden;
}

.nameText {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 14px;
  color: #212121;
}

.cellFavorite {
  width: 36px;
  min-width: 36px;
  max-width: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.cellDate {
  width: 120px;
  min-width: 120px;
  max-width: 120px;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding-right: 14px;
  flex-shrink: 0;
}

.cellOwner {
  width: 110px;
  min-width: 110px;
  max-width: 110px;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding-right: 14px;
  flex-shrink: 0;
}

.cellSize {
  width: 70px;
  min-width: 70px;
  max-width: 70px;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  flex-shrink: 0;
}

.cellType {
  width: 80px;
  min-width: 80px;
  max-width: 80px;
  font-size: 13px;
  color: #757575;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  flex-shrink: 0;
}

.cellTags {
  width: 130px;
  min-width: 130px;
  max-width: 130px;
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: nowrap;
  overflow-x: auto;
  overflow-y: hidden;
  padding-right: 14px;
  flex-shrink: 0;
  -ms-overflow-style: none;
  scrollbar-width: none;
}

.cellTags::-webkit-scrollbar {
  display: none;
}

.tag {
  display: inline-flex;
  align-items: center;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 500;
  background-color: #f0f4f8;
  color: #334155;
  border: 1px solid #e2e8f0;
  white-space: nowrap;
  transition: all 0.2s ease;
}

.cellSharing {
  width: 50px;
  min-width: 50px;
  max-width: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.cellDelete {
  width: 40px;
  min-width: 40px;
  max-width: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #757575;
  cursor: pointer;
  flex-shrink: 0;
}

.cellDelete:hover {
  color: #d32f2f;
}

.cellSpacer {
  flex: 1;
  min-width: 0;
}

/* ---- Icons ---- */
.folderIcon {
  color: #ffc107;
  font-size: 24px !important;
}

.fileIcon {
  font-size: 22px !important;
}

.pdfIcon {
  color: #f44336;
}

.csvIcon {
  color: #4caf50;
}

.imgIcon {
  color: #9c27b0;
}

.txtIcon {
  color: #607d8b;
}

.docIcon {
  color: #2196f3;
}

/* Favorite Star */
.favoriteIcon {
  cursor: pointer;
  font-size: 20px !important;
  transition: color 0.15s ease, transform 0.15s ease;
}

.favoriteIcon:hover {
  transform: scale(1.15);
}

.favoriteActive {
  color: #ffc107 !important;
}

.favoriteInactive {
  color: #bdbdbd !important;
}

.favoriteInactive:hover {
  color: #ffc107 !important;
}

/* Sharing icons */
.sharingIcon {
  font-size: 20px !important;
  color: #9e9e9e;
}

.personalIcon {
  color: #1976d2;
}

.teamIcon {
  color: #2e7d32;
}

/* Header cell text (kept for backward compat) */
.headerCell {
  cursor: inherit;
}

/* Empty state */
.emptyState {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 200px;
  color: #9e9e9e;
}
