                                            onDoubleClick={() => {
                                                if (isFolder) {
                                                    if (folderClickTimerRef.current) {
                                                        clearTimeout(folderClickTimerRef.current);
                                                        folderClickTimerRef.current = null;
                                                    }
                                                    handleFolderDoubleClick(entry.item as IFolder);
                                                } else {
                                                    handleFileDoubleClick(entry.item as IFolderFile);
                                                }
                                            }}
