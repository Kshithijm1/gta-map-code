  const [customTagInput, setCustomTagInput] = useState("");

  const handleAddCustomTag = () => {
    const trimmed = customTagInput.trim();
    if (trimmed) {
      addCustomTag(index, trimmed);
      setCustomTagInput("");
    }
  };
